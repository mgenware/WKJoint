{
  "name": "wkjoint",
  "version": "1.0.0",
  "description": "",
  "main": "runtime.ts",
  "scripts": {
    "compile": "tsc",
    "dev": "tsc -w",
    "lint": "tslint --project tsconfig.json --config tslint.json",
    "uglify": "uglifyjs ./dist/wkj_runtime.js -cmo ./dist/wkj_runtime.min.js",
    "build": "yarn lint && yarn compile && yarn uglify"
  },
  "author": "Mgen",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "tslint": "^5.8.0",
    "typescript": "^2.6.2",
    "uglify-js": "^3.2.2"
  }
}
